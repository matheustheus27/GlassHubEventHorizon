<UserControl x:Class="GlassHub.EventHorizon.GUI.Views.InspectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:GlassHub.EventHorizon.GUI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:InspectViewModel}"
             d:DesignHeight="600" d:DesignWidth="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="Auto"/> <!-- Picker -->
            <RowDefinition Height="Auto"/> <!-- 4 Stat Cards -->
            <RowDefinition Height="*"/>    <!-- Search & Entries List -->
        </Grid.RowDefinitions>

        <!-- Header Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Inspetor &amp; Análise de Telemetria" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="Inspecione estruturas internas, verifique integridade CRC e audite taxas de compressão sem extrair no disco." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
        </StackPanel>

        <!-- Archive Picker Toolbar -->
        <Border Grid.Row="1" Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="12" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding SourcePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,0"/>
                <Button Grid.Column="1" Content="Procurar..." Command="{Binding BrowseSourceCommand}" Margin="0,0,6,0" Padding="12,6"/>
                <Button Grid.Column="2" Content="🔍 Inspecionar" Command="{Binding InspectCommand}" Style="{StaticResource PrimaryActionButtonStyle}" Margin="0,0,6,0" Padding="14,6"/>
                <Button Grid.Column="3" Content="🛡️ Testar Integridade" Command="{Binding VerifyIntegrityCommand}" Padding="12,6"/>
            </Grid>
        </Border>

        <!-- 4 Cosmic Stat Glass Cards -->
        <Grid Grid.Row="2" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Card 1: Entries -->
            <Border Grid.Column="0" Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,0,4,0">
                <StackPanel>
                    <TextBlock Text="ENTRADAS TOTAIS" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding EntryCountText}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Card 2: Uncompressed Size -->
            <Border Grid.Column="1" Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12" Margin="3,0,3,0">
                <StackPanel>
                    <TextBlock Text="TAMANHO ORIGINAL" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding UncompressedSizeText}" Foreground="{DynamicResource NeonCyanBrush}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Card 3: Compression Ratio -->
            <Border Grid.Column="2" Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12" Margin="3,0,3,0">
                <StackPanel>
                    <TextBlock Text="TAXA COMPRESSÃO" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding CompressionRatioText}" Foreground="{DynamicResource NeonMagentaBrush}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <!-- Card 4: Integrity Status -->
            <Border Grid.Column="3" Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12" Margin="4,0,0,0">
                <StackPanel>
                    <TextBlock Text="INTEGRIDADE CRC" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding IntegrityStatusText}" Foreground="{Binding IsIntegrityPassed, Converter={StaticResource IntegrityBrushConv}}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Searchable Archive Content Registry -->
        <Border Grid.Row="3" Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Conteúdo do Arquivo:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" Padding="8,4"/>
                </Grid>

                <Border Grid.Row="1" Background="{DynamicResource BackgroundDarkBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding FilteredEntries}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="8,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="0" Data="{StaticResource IconFile}" Fill="{DynamicResource NeonCyanBrush}" Width="12" Height="12" Stretch="Uniform" VerticalAlignment="Center" Margin="4,0,8,0"/>
                                        <TextBlock Grid.Column="1" Text="{Binding}" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
