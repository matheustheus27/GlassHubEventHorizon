<UserControl x:Class="GlassHub.EventHorizon.GUI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:GlassHub.EventHorizon.GUI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
             d:DesignHeight="600" d:DesignWidth="700">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="0,0,10,0">
            <!-- Header Title -->
            <TextBlock Text="Configurações &amp; Preferências do Sistema" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="18" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="Personalize a aparência cósmica, limites de hardware e integrações do Windows Explorer." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,16"/>

            <!-- Section 1: Appearance & Identity -->
            <Border Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="🎨 IDENTIDADE VISUAL &amp; TEMA" Foreground="{DynamicResource NeonCyanBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Tema Visual Cósmico:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                    <ComboBox SelectedIndex="{Binding SelectedThemeIndex}" Margin="0,0,0,14">
                        <ComboBoxItem Content="🌌 GlassHub Cosmic Dark (Assinatura Oficial)"/>
                        <ComboBoxItem Content="☀️ Fluent Light (Windows 11 Nativo)"/>
                        <ComboBoxItem Content="⚙️ WinRAR Cyber (Metal Escuro)"/>
                        <ComboBoxItem Content="🟣 Neon Cyberpunk (Vibrante)"/>
                        <ComboBoxItem Content="💻 Glass Dark (VS Code / GitHub)"/>
                    </ComboBox>

                    <TextBlock Text="Idioma da Interface:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                    <ComboBox SelectedIndex="{Binding SelectedLanguageIndex}">
                        <ComboBoxItem Content="🇧🇷 Português (Brasil)"/>
                        <ComboBoxItem Content="🇺🇸 English (United States)"/>
                    </ComboBox>
                </StackPanel>
            </Border>

            <!-- Section 2: Engine & Processing Limit -->
            <Border Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="⚡ MOTOR &amp; DESEMPENHO DE CPU" Foreground="{DynamicResource NeonCyanBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>

                    <TextBlock Text="Motor de Compressão Preferencial:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                    <ComboBox SelectedIndex="{Binding PreferredEngineIndex}" Margin="0,0,0,14">
                        <ComboBoxItem Content="Chaveamento Inteligente (Smart Fallback: .NET + 7-Zip CLI)"/>
                        <ComboBoxItem Content="Motor Nativo .NET (System.IO.Compression)"/>
                        <ComboBoxItem Content="Motor 7-Zip Engine CLI"/>
                    </ComboBox>

                    <Grid Margin="0,0,0,4">
                        <TextBlock Text="Limite de Núcleos / Threads Simultâneas:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11"/>
                        <TextBlock Text="{Binding CpuThreadsLimit}" Foreground="{DynamicResource NeonCyanBrush}" FontSize="11" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </Grid>
                    <Slider Minimum="1" Maximum="{Binding MaxCpuThreads}" Value="{Binding CpuThreadsLimit}" TickFrequency="1" IsSnapToTickEnabled="True"/>
                </StackPanel>
            </Border>

            <!-- Section 3: Paths & OS Integration -->
            <Border Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="📂 DIRETÓRIOS &amp; INTEGRAÇÃO COM O SISTEMA" Foreground="{DynamicResource NeonCyanBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>

                    <TextBlock Text="Diretório Padrão para Extração / Saída:" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding DefaultExtractionPath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,6,0"/>
                        <Button Grid.Column="1" Content="Procurar..." Command="{Binding BrowseDefaultPathCommand}" Padding="12,6"/>
                    </Grid>

                    <CheckBox IsChecked="{Binding IntegrateExplorer}" Margin="0,4,0,0">
                        <TextBlock Text="Integrar opções do GlassHub no Menu de Contexto do Windows Explorer" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12"/>
                    </CheckBox>
                </StackPanel>
            </Border>

            <!-- Section 4: About GlassHub Ecosystem -->
            <Border Background="{DynamicResource CardGlassBrush}" BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Path Grid.Column="0" Data="{StaticResource IconGlassHubLogo}" Fill="{DynamicResource NeonCyanBrush}" Width="32" Height="32" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="GlassHub EventHorizon v1.0.0" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="13" FontWeight="Bold"/>
                        <TextBlock Text="Sistema de Alta Performance de Compressão Cósmica com Glassmorphism." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                        <TextBlock Text="Desenvolvido sob o ecossistema GlassHub." Foreground="{DynamicResource NeonCyanBrush}" FontSize="10" FontWeight="SemiBold" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
